stages:
  - build

build:
  stage: build
  tags:
    - shell
  script:
    - echo "Starting build..."
    - node --version
    - npm --version
    - npm install
    - node --check src/server.mjs
    - echo "Build completed successfully"
