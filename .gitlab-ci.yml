stages:
  - build

build:
  stage: build
  tags:
    - shell
  script:
    - Write-Host "Starting build process..."
    - node --version
    - npm --version
    - npm install
    - node --check src/server.mjs
    - Write-Host "Build completed successfully!"
